import { CreatePurchaseDtoTs } from './create-purchase.dto.ts';

describe('CreatePurchaseDtoTs', () => {
  it('should be defined', () => {
    expect(new CreatePurchaseDtoTs()).toBeDefined();
  });
});
