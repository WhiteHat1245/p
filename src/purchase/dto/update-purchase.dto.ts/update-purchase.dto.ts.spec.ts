import { UpdatePurchaseDtoTs } from './update-purchase.dto.ts';

describe('UpdatePurchaseDtoTs', () => {
  it('should be defined', () => {
    expect(new UpdatePurchaseDtoTs()).toBeDefined();
  });
});
